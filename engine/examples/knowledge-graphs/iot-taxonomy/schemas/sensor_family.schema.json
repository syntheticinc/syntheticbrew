{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sensor_family",
  "title": "Sensor Family",
  "description": "A family of IoT sensors with similar measurement characteristics.",
  "type": "object",
  "x-id-field": "code",
  "x-tool-expose": ["list", "get"],
  "x-tool-description": "Families of IoT sensors. Use list_sensor_family to discover what physical signals can be measured, get_sensor_family for a single family's details.",
  "required": ["code", "name", "measures"],
  "additionalProperties": false,
  "properties": {
    "code": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{1,30}$",
      "description": "Lowercase snake_case identifier.",
      "x-index": true
    },
    "name": {
      "type": "string",
      "minLength": 3,
      "maxLength": 60
    },
    "measures": {
      "type": "string",
      "description": "What the sensor measures (water, temperature, occupancy, etc.)",
      "x-index": true
    },
    "typical_battery_months": {
      "type": "integer",
      "minimum": 0,
      "maximum": 240
    },
    "notes": {
      "type": "string",
      "maxLength": 500
    }
  }
}
